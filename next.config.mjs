/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'anotherdomain.com', 'example.com'],
      },
};

export default nextConfig;
